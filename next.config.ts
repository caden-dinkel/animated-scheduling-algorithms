import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
};

// module.exports = {
//   // Optional: Enable the `basePath` if your app is in a subfolder on GitHub Pages
//   basePath: "/animated-scheduling-algorithm",

//   // Optional: Asset Prefix to ensure that assets are loaded correctly
//   assetPrefix: "/animated-scheduling-algorithm/",
// };

export default nextConfig;
